SET(UDJ_SRCS main.cpp MetaWindow.cpp)
SET(UDJ_HDRS MetaWindow.hpp)

QT4_WRAP_CPP(UDJ_HDRS_MOC ${UDJ_HDRS})
INCLUDE(${QT_USE_FILE})
ADD_DEFINITIONS(${QT_DEFINITIONS})

ADD_EXECUTABLE(udj ${UDJ_SRCS} ${UDJ_HDRS_MOC})
TARGET_LINK_LIBRARIES(udj ${QT_LIBRARIES})
